# 🛠 관리자 운영 가이드

2026년 보훈사적지 탐방 프로그램 미션 — 카메라 웹앱

---

## 📌 핵심 자원 한눈에 보기

| 구분 | 위치 |
|---|---|
| 학생 접속 URL | https://crimsonheartkr.github.io/veterans-mission-camera-2026/ |
| 프레임 폴더 (PNG 업로드) | https://drive.google.com/drive/folders/1yk8EQ68XpoOm8GYUJbguYgY2NftdVd5B |
| 제출 기록 스프레드시트 | https://docs.google.com/spreadsheets/d/1PVFXUE3nAuAkcm49i12E7K7wl-QprR07VrfOrh0PBBs/edit |
| 사진 저장 위치 | 프레임 폴더 안의 `제출사진/` 하위 폴더 (자동 생성됨) |
| 소스코드 | https://github.com/CrimsonHeartKR/veterans-mission-camera-2026 |

---

## 1️⃣ 행사 전 — 1회만 (10분)

### A. 프레임 PNG 업로드
1. 프레임 폴더 열기
2. 프로그램용 PNG 프레임을 드래그해서 업로드
3. 각 PNG 우클릭 → **공유** → **링크가 있는 인터넷의 모든 사용자** → 뷰어 권한
   - 이 단계가 빠지면 학생 화면에서 프레임이 회색으로 보일 수 있습니다
4. 파일명이 그대로 화면 라벨이 됩니다 (예: `1.png` → `1`, `현충원_프레임.png` → `현충원_프레임`)
5. **가로형(landscape) 프레임 — 파일명에 `가로` 키워드를 포함시키면 자동으로 가로(4:3)로 출력됩니다**
   - 예) `현충원_가로.png`, `가로_단체사진.png`, `애국지사_가로.png`
   - 가로 프레임이 선택되면 카메라 미리보기와 최종 저장 사진 모두 가로 비율(4:3)로 합성됩니다
   - 학생은 별도 회전 조작 없이 그대로 촬영하면 됩니다 (앱 UI 자체는 항상 세로 고정)

### B. 스프레드시트 초기 상태 확인
- 처음 한 명이 제출하면 `제출목록` 탭이 자동 생성되고 헤더가 자동으로 박힙니다.
- 미리 시트를 만들어둘 필요는 없습니다.

### C. 학생용 안내물 준비
- 위 **학생 접속 URL**을 QR로 만들어 인쇄 (네이버 QR / qrcode-monkey.com 등 무료 서비스 가능)
- 학생들에게 안내할 내용:
  > 1) QR을 휴대폰 카메라로 스캔
  > 2) 사이트가 열리면 **카메라 권한 허용**
  > 3) 프레임 선택 → 촬영 → 학교/팀/팀원/정답 입력 → **제출하기**

---

## 2️⃣ 행사 중 — 실시간 모니터링

### 제출이 잘 들어오는지
- **스프레드시트** `제출목록` 탭 새로고침
- 한 행 = 한 팀 제출
- `사진URL` 컬럼의 링크를 클릭하면 해당 사진을 바로 미리보기 가능

### 사진이 잘 저장되는지
- 프레임 폴더 → `제출사진/` 폴더에서 시간순으로 누적 확인
- 파일명 규칙: `보훈미션_{팀명}_{날짜}_{타임스탬프}.jpg`

### 학생이 "제출이 안 돼요" 라고 할 때
1. 토스트가 뭐라고 떴는지 물어보기
   - **"✅ 미션이 제출되었습니다"** → 정상. 스프레드시트 새로고침 필요
   - **"제출 실패 — 로컬 저장으로 전환합니다"** → 네트워크 또는 Apps Script 문제 (아래 5번 트러블슈팅)
   - **"모든 항목을 입력해주세요"** → 4칸 중 빈 칸 있음
2. 학생 폰의 인터넷 연결 확인 (와이파이/데이터)

---

## 3️⃣ 행사 후 — 데이터 정리

### 결과 확인
- 스프레드시트 `제출목록` 탭에서 팀별 제출 현황 한눈에 보기
- 필터/정렬은 시트 상단 메뉴에서 자유롭게

### 일괄 다운로드
- **사진 일괄**: 프레임 폴더 → `제출사진/` 폴더 우클릭 → **다운로드** → ZIP으로 받음
- **제출 기록**: 스프레드시트 → 파일 → 다운로드 → Excel/CSV

### 다음 행사를 위한 초기화 (선택)
- 새 행사로 다시 쓰려면:
  1. 스프레드시트 `제출목록` 탭에서 헤더(1행)를 제외한 데이터만 선택 → 삭제
  2. `제출사진/` 폴더 안의 사진들을 다른 곳으로 이동 또는 삭제
- 또는 매 행사마다 새 스프레드시트/폴더를 만들어 운영해도 됩니다 (이 경우 [코드 수정 필요](#-id-바꾸는-법) — 하단 참조).

---

## 4️⃣ 자주 쓰는 체크리스트

| 시점 | 확인할 것 |
|---|---|
| 행사 전날 | 학생 URL이 모바일에서 열리는지, 카메라 권한 허용 후 셔터가 잘 작동하는지 1회 테스트 제출 |
| 행사 시작 1시간 전 | 프레임 폴더에 PNG가 다 올라와 있고 공유 권한이 "링크 있는 모든 사용자"인지 |
| 행사 진행 중 | 스프레드시트에 제출이 누적되고 있는지 (10~15분 간격으로 새로고침) |
| 행사 종료 후 | `제출사진/` 폴더 사진 수 ≈ 스프레드시트 제출 행 수 인지 (불일치 시 누락 의심) |

---

## 5️⃣ 트러블슈팅

### 🚨 "제출 실패" / "서버 권한 설정 오류" / 시트가 비어있음 (가장 흔한 문제)

**원인**: Apps Script 배포의 **액세스 권한이 "모든 사용자"가 아님** → 학생 브라우저가 로그인 페이지로 튕겨나면서 fetch 실패.

**해결 방법 — Apps Script 재배포 (5분)**:

1. https://script.google.com/ 열기 → 본 프로젝트 클릭
2. 우측 상단 **배포 → 배포 관리** 클릭
3. 활성 배포 옆 ✏️ **연필 아이콘(수정)** 클릭
4. **버전**: `새 버전` 선택
5. **다음 사용자로 실행**: `나` (본인 계정)
6. **액세스 권한**: ⚠️ **모든 사용자** ← 이게 핵심!
   - "Google 계정이 있는 모든 사용자" 가 아니라 **"모든 사용자"** 여야 함
   - 영문 UI라면 **Anyone** (Anyone with Google account 아님)
7. **배포** 클릭 → 권한 승인 다이얼로그가 뜨면 통과 (계정 선택 → "고급" → "안전하지 않음(unsafe)" → "허용")
8. 배포가 끝나면 웹앱 URL 표시됨 — **URL이 그대로면 재배포 끝**, 만약 URL이 바뀌었다면 새 URL을 알려주세요

### 빠른 자가 진단 ① — Apps Script GET 응답 확인 (프레임 읽기)

브라우저 새 탭에 다음 URL을 직접 입력:

```
https://script.google.com/macros/s/AKfycbzGOFewu5D_Te9gFsxykD99c5QYqKGVq8WVoLgyBQQX9yaKNu4CFvJoIZri3ph-ypgV/exec?action=getFrames
```

- ✅ JSON `{"success":true,"frames":[...]}` 가 보이면 **권한 정상**
- ❌ "액세스 거부됨" 또는 Google 로그인 페이지가 뜨면 위 절차로 재배포 필요

### 빠른 자가 진단 ② — POST 제출 경로 확인 (스프레드시트 쓰기 권한) ⭐

**스프레드시트에 데이터가 안 들어올 때 가장 먼저 확인할 것**입니다. POST는 GET과 달리 Drive 쓰기 + Sheets 쓰기 권한이 추가로 필요한데, 권한 다이얼로그를 빠뜨리고 넘어갔을 가능성이 있습니다.

1. https://script.google.com/ 에서 본 프로젝트 열기
2. 좌측 상단 **함수 드롭다운**에서 `_testSubmit` 선택
3. **▶ 실행** 클릭
4. **권한 다이얼로그가 뜨면 모두 허용** (계정 선택 → 고급 → "안전하지 않음" → "모두 허용") ← 이게 핵심
5. 좌측 **시계 아이콘(실행)** 또는 하단 "실행 로그" 펼쳐 결과 확인:
   - 로그에 `success:true` 와 `photoUrl: ...` 이 보이면 **정상** ✓
   - `error` 가 보이면 메시지를 그대로 알려주세요 (예: "권한이 없습니다", "스프레드시트를 찾을 수 없습니다" 등)
6. 스프레드시트(`1PVFXU...`)를 열어 **"__진단_학교"** 행이 들어왔는지 확인
   - 있으면 백엔드 OK — 학생 화면에서 다시 제출 테스트
   - 없으면 로그 메시지로 원인 추적

이 절차로 한 번 권한을 통과시키면, 이후 학생 제출도 자동으로 성공하기 시작합니다.

### "프레임을 불러올 수 없습니다"
- 위와 동일한 권한 이슈일 가능성 높음 — 자가 진단 URL로 확인
- 또는 프레임 폴더의 PNG가 "링크 있는 모든 사용자" 공유로 설정되지 않은 경우

### 학생이 제출했는데 시트에 안 들어옴
- **원인 1**: Apps Script 권한 승인이 안 됐을 때 → script.google.com 에서 함수 한 번 직접 실행해 권한 다이얼로그를 통과시키기 (`_testGetFrames` 함수 실행)
- **원인 2**: 학생이 토스트를 잘못 본 경우 → "로컬 저장으로 전환되었습니다" 가 떴다면 사진은 학생 폰에만 저장됨. 학생에게 다시 시도 요청

### 사진이 회색/엑스로 보임 (학생 화면에서 프레임 미리보기)
- **원인**: PNG의 공유 권한이 "제한됨"일 때
- **해결**: 해당 PNG 우클릭 → 공유 → 링크가 있는 모든 사용자 → 뷰어

### 카메라가 안 열림
- HTTPS 연결인지 확인 (GitHub Pages는 항상 HTTPS이므로 정상)
- 학생 폰에서 다른 사이트의 카메라 권한이 차단된 상태인지 확인 (브라우저 설정 → 사이트 권한)

---

## 🔧 ID 바꾸는 법 (다른 행사 운영 시)

다른 폴더/스프레드시트로 운영하려면 두 군데를 수정합니다:

### 1) `apps-script/Code.gs` 의 상단 상수
```js
const FRAME_FOLDER_ID = '새-드라이브-폴더-ID';
const SHEET_ID        = '새-스프레드시트-ID';
```
→ Apps Script 편집기에서 수정 → 저장 → **배포 → 배포 관리 → 수정 → 새 버전** 발행

### 2) `index.html` 상단 `appState` (선택 — 표시용)
```js
frameFolderId: '...',
spreadsheetId: '...',
```
→ git push 하면 GitHub Pages가 자동 재배포

---

## 문의/장애

- 코드 레벨 문제는 [GitHub Issues](https://github.com/CrimsonHeartKR/veterans-mission-camera-2026/issues) 에 기록
- 운영 중 긴급 상황: 모든 학생을 일단 **사진 저장 → 나중에 일괄 업로드** 모드로 안내해도 데이터 손실 없음 (학생 폰에 사진 + 클립보드에 입력값이 보관됨)
